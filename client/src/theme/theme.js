import { createTheme } from '@mui/material/styles';

const theme = createTheme({
	palette: {
		primary: {
			main: '#fd831f',
		},
		secondary: {
			main: '#06407c',
		},
		defult: {
			main: '#757575',
		},
	},
});

export default theme;
